# Maven build files
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# IDE files
.idea/
*.iml
*.ipr
*.iws
.gradle/
build/

# Test files
*.class
*.log

# Test data
libobfuscated*.so
*.patch
*.txt

# Ignore SO files in lib directory
lib/*.so
deobfuscation-core/lib/*.so

# Ignore analysis reports and temporary files in tools
tools/*.json
tools/*.so
tools/*.patch
tools/*.txt

# OS files
.DS_Store
Thumbs.db

# Keep project-level config files but don't commit secrets
.claude/settings.local.json  # User-specific local settings (keep out of git)
.claude/.mcp.json            # Project-level MCP config (keep in git)
claude.code                 # Claude Code session files
